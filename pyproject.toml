[project]
name = "kubernetes-crds"
version = "0.1.0"
description = "Kubernetes CRDs statistics"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kubernetes>=36.0.2",
]

[dependency-groups]
dev = [
    "pyright>=1.1.411",
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]
